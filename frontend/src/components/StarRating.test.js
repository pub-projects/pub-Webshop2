import React from 'react';
import { shallow, mount, render } from 'enzyme';

import { StarRating } from './StarRating';

const fakeData = {
    "rating": {
        "5": 23,
        "4": 18,
        "3": 4,
        "2": 10,
        "1": 12
    }
};

describe('controls star rating calculations', () => {
    it('total number of star ratings (=67)', () => {

    });
});