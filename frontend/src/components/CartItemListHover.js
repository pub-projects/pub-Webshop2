import { CartConsumer } from '../util/CartContext';
import { ProductConsumer } from '../util/ProductsContext';

export const CartItemListHover = () => {

    return (
        <div className="cart-list-hover-wrapper">
            Cart List Hover
        </div>
    )
}